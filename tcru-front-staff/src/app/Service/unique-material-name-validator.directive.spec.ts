import { Uniquematerial_nameValidatorDirective } from './unique-material-name-validator.directive';

describe('Uniquematerial_nameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new Uniquematerial_nameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
